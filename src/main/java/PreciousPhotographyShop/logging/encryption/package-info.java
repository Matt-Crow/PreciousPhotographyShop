/**
 * Just use EncryptionProvider.createDefaultEncrypter()
 * 
 * All of these assume the use of 16 byte AES keys, so be sure to CTRL-f "AES" 
 * and 16 if you change that
 */
package PreciousPhotographyShop.logging.encryption;
